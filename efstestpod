apiVersion: v1
kind: Pod
metadata:
  name: efs-test
spec:
  containers:
  - name: test
    image: nginx
    volumeMounts:
    - mountPath: /data
      name: efs-vol
  volumes:
  - name: efs-vol
    persistentVolumeClaim:
      claimName: efs-pvc
